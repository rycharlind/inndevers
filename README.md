# Inndevers Web App (React.JS + Webpack)

## Get Started
1. Download and install Node.JS and NPM (https://nodejs.org)
2. Use NPM to install Web Pack (https://webpack.github.io/) 
**. `npm install webpack --save`
